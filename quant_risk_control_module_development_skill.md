# quant_risk_control_module_development_skill

## 一、Skill 用途与适用范围

本 Skill 为**量化框架风控模块标准化开发规范**，专供 Claude Code 进行代码生成、逻辑编写、Bug 修复、双场景适配、功能迭代。所有代码逻辑、分支判断、执行流程、场景差异必须完全遵循本 Skill，确保输出代码统一、可落地、回测/实盘逻辑完全对齐。

**适用开发内容**：风控指标计算、规则引擎校验、止盈止损执行、回测适配层、实盘适配层、风控状态机、日志告警、权限锁定、容错重试逻辑。

**强制约束**：所有核心风控判定逻辑、阈值触发条件、优先级顺序、状态流转，**禁止自定义发挥**，必须100%匹配本 Skill 定义。

## 二、核心开发总原则（强制）

- **双场景逻辑一致性原则**：风控指标计算、规则阈值、触发条件、优先级、状态机逻辑，回测/实盘**完全一致**；仅执行层、容错层、数据来源做场景差异化适配。

- **实时逐帧检测原则**：禁止收盘统一结算式风控，必须实现 **每Tick/每K线更新 → 指标刷新 → 规则校验 → 即时执行**。

- **风控优先拦截原则**：所有开仓、持仓变更动作，必须先过风控校验，再进入交易执行层。

- **状态幂等原则**：风控触发后做状态锁定，避免同一风险事件重复平仓、重复告警、重复锁仓。

- **分层解耦原则**：严格遵循六层架构，禁止跨层调用、禁止策略逻辑侵入风控层。

## 三、标准化架构与代码分层（必须严格遵守）

代码工程必须分为固定六层结构，包/类命名可微调，层级职责不可变更。

### 3\.1 数据接入层

职责：统一行情、资金、持仓、委托数据接入、清洗、时序对齐、字段标准化。

差异化：

- 实盘：接收交易所 Tick、K线、持仓推送、资金推送、委托状态推送。

- 回测：接收历史回放 Tick/K线、模拟持仓、模拟资金快照。

输出：统一结构化数据（价格、持仓量、可用资金、持仓市值、盈亏、时间戳）。

### 3\.2 风控计算层（核心计算，无业务分支）

纯计算层，无执行逻辑，只输出指标快照，供规则引擎调用。

必须实现指标：

- 单笔持仓盈亏、浮动盈亏比例

- 策略当日总盈亏、累计盈亏

- 策略当前最大回撤、当日回撤

- 总仓位占比、单标的仓位占比

- 持仓时长、距开仓周期数

- 成交滑点偏差值、价格波动幅度

- 连续亏损笔数、连续亏损周期数

约束：所有指标带**时间戳快照**，支持回放、复盘追溯。

### 3\.3 规则引擎层（核心判定逻辑）

加载用户/默认风控参数，基于指标快照做阈值判定，输出风控事件与对应动作。

能力：支持多规则并存、优先级仲裁、状态幂等锁定。

### 3\.4 执行调度层

接收规则引擎事件，区分场景执行：回测模拟执行 / 实盘真实交易执行。

统一动作池：拒单、部分平仓、全部平仓、暂停开仓、永久锁仓、风控告警。

### 3\.5 场景适配层

隔离所有场景差异：滑点模拟、执行延迟、容错重试、状态恢复机制。

### 3\.6 日志监控层

全量事件落库：触发时间、指标快照、阈值、动作、执行结果、场景标记。

## 四、风控规则引擎：完整可编码规则

所有规则**回测/实盘共用同一套判定代码**。

### 4\.1 价格盈亏风控（止盈止损核心）

- **固定止盈止损**：持仓浮动盈亏/比例 ≥ 止盈阈值 → 触发止盈平仓；浮动亏损 ≥ 止损阈值 → 触发止损平仓。

- **动态追踪止盈**：价格创新高后动态抬升止盈线，回落触发平仓；仅锁定浮动收益，不主动提前止损。

- **阶梯止盈止损**：多档位阈值匹配，依次执行部分平仓、全平。

### 4\.2 回撤与亏损风控

- 最大浮亏回撤超阈值 → 减仓 \+ 暂停新开仓

- 当日累计亏损超阈值 → 禁止开仓、可配置一键清仓

- 连续N笔/连续N周期亏损 → 冻结交易权限

### 4\.3 仓位风控

- 总仓位占比超限 → 拦截新开仓

- 单标的仓位超限 → 拦截该标的开仓

- 单笔开仓手数、单日开仓次数超限 → 直接拒单

### 4\.4 异常交易风控

- 滑点偏差超阈值 → 记录异常 \+ 可配置拦截成交

- 价格大幅跳空/涨跌停 → 撤单 \+ 暂停当期交易

- 废单过多、重复委托、超时未成交 → 短时禁单保护

### 4\.5 时间周期风控

- 持仓时长超最大持有周期 → 强制平仓

- 非允许交易时段 → 拦截开仓

## 五、风控优先级编码规则（固定、不可修改）

多规则同时触发时，按以下优先级执行，高优先级覆盖低优先级动作：

**L1 紧急拦截（最高）**：价格异动、委托异常、系统故障、合规风控 → 立即终止交易、撤单锁仓

**L2 重度风险止损**：单日大幅亏损、最大回撤超标、持仓超时强制平仓

**L3 盈亏调控**：单笔止盈止损、动态追踪止盈、阶梯平仓

**L4 额度管控（最低）**：仓位超限、手数超限、时段限制、新开仓拦截

## 六、回测场景专属编码约束

Claude Code 编写回测逻辑必须遵守以下约束，杜绝回测虚优：

- 必须实现 **逐Tick/逐K线时序遍历风控**，禁止收盘一次性结算

- 止盈止损成交必须模拟滑点、模拟执行延迟，不默认最优价格成交

- 风控状态全局持久化：暂停开仓、锁仓状态跨周期生效，直至解除

- 完整复刻优先级逻辑，保证多规则冲突结果与实盘一致

- 输出专属风控报表指标：触发次数、止盈止损命中率、回撤控制统计、风险事件分布

## 七、实盘场景专属编码约束

- 风控计算与校验逻辑异步独立线程，**不阻塞主交易链路**

- 指标更新毫秒级，高频场景整体延迟 ≤ 10ms

- 三层执行闭环：开仓预拦截、持仓事中处置、重度风险事后锁仓

- 失败重试机制：平仓指令失败自动重试，重试超时触发告警

- 断连容错：离线缓存风控状态，重连后续跑校验，不丢失风控节点

- 分级告警 \+ 人工干预接口：支持手动暂停/恢复风控、手动解锁、手动平仓

## 八、统一业务状态机（必须编码实现）

策略全局风控状态枚举：

- NORMAL：正常交易（可开仓、可平仓）

- LIMIT\_OPEN：禁止新开仓（可平仓减仓）

- PAUSE\_TRADE：暂停全部交易

- LOCKED：重度风控锁定，需人工解锁

状态变更必须落日志、带时间戳、带触发规则ID。

## 九、回测/实盘差异化编码对照表（Claude Code 分支判断依据）

|维度|回测逻辑|实盘逻辑|统一不变逻辑|
|---|---|---|---|
|检测频率|逐Tick/逐K线时序遍历|毫秒级Tick实时轮询|指标计算、阈值判定、优先级一致|
|交易执行|模拟撮合、滑点模拟、延迟模拟|交易所真实下单、撤单、成交|触发条件、执行动作、状态变更一致|
|容错机制|无异常容错，纯逻辑校验|断连缓存、指令重试、超时告警|风控判定结果完全对齐|
|输出结果|风控统计报表、回测指标修正|实时告警、运维日志、资金安全兜底|风控事件数据结构一致|

## 十、非功能开发强制标准

- **性能**：实盘单次风控全链路校验延迟 ≤10ms；回测批量计算不阻塞遍历流程。

- **稳定性**：无漏判、无错判、无重复执行；状态幂等、快照可追溯。

- **扩展性**：规则插件化，新增指标/动作不改动核心引擎代码。

- **可观测**：所有风控事件可查询、可统计、可复盘。

## 十一、禁止行为（编码红线）

- 禁止回测使用收盘统一风控，必须逐帧检测

- 禁止实盘与回测判定逻辑不一致

- 禁止低优先级规则覆盖高优先级规则

- 禁止策略业务逻辑侵入风控计算层与规则引擎层

- 禁止风控触发无状态锁定、无日志快照、无幂等处理