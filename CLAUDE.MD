# 项目工作协议

## 项目说明
- alphaQuantSystem是一个量化交易框架，该框架现已经完成了数据接入（基于qmt/akshare/东方财富api等）、实盘交易（qmt）、策略引擎等模块，要按照下面的要求对它进行重构，实现一个基于事件驱动的交易框架


## 目录说明

```
alphaQuantSystem/
├── core/                # 【核心层】事件引擎、标准数据模型、全局定义
├── gateway/             # 【接入层】QMT专属网关（行情+交易）
├── data/                # 【数据层】双数据源管理、Backtrader数据适配
├── strategy/            # 【策略层】统一策略模板、策略引擎
├── backtest/            # 【回测层】Backtrader封装集成
├── analyze/             # 【绩效层】QuantStats绩效分析
├── trader/              # 【实盘层】QMT实盘交易引擎
├── risk/                # 【风控层】实盘/回测风控规则
├── agent/               # 【高级AI层】Trading Agent智能分析（预留接口）
├── monitor/             # 【监控层】日志、告警、状态监控
├── ui/                  # 【交互层】Web监控面板
├── utils/               # 【工具层】通用工具函数
├── config/              # 【配置层】全局配置文件
├── main.py              # 框架启动入口
└── requirements.txt     # 依赖包清单
```
## 要遵循的内容
 -- 项目遵循减少重复造轮子思路，实盘交易使用qmt，回测模块使用自建事件驱动引擎，绩效评估使用统一 metrics + QuantStats，数据使用qmt/akshare，股票分析使用trading agent（预留接口，待后续开发）
 -- 不要随意改动项目现在的命名风格
 -- 评估测试项目已完成内容的代码质量，保留可用的，或者在其基础上优化
 -- 开发语言：Python 3.8~3.10
 -- 架构规范：事件驱动、模块化、松耦合、工程化
 -- 事件驱动借鉴veighna项目
 -- 所有订单都要先过风控
 -- 每次回复时都叫我南哥
 -- 用中文回答
 -- 涉及到模块，类间调用时要写注释
 -- 范围限制：请只关注和处理与本项目相关的文件，不要盲目查找或读取其他无关语言的文件

## 核心行为准则
 -- 禁止全局搜索：为了节省计算资源，除非我明确下达“全局搜索”或“grep”等指令，否则绝对禁止扫描整个工作区。你只能精准读取我提供给你的特定文件路径。
 -- 先审批，后动手：每次遇到修改代码、修复 Bug 或开发新功能的需求时，请先用简短的话告诉我你的“修改思路”以及“准备修改哪些文件的第几行”。**必须等我明确回复“同意”后，你才能调用编辑工具真实修改本地代码。**
 -- 极简输出：不要说废话，不要道歉，不要解释基础常识。请直接给出结论或核心代码，最大程度节省冗余的输出消耗


## 技术栈
### 后端
  -- 回测引擎：Backtrader 3.2.0
  -- 绩效分析：QuantStats 0.0.63
  -- 实盘交易：QMT xtquant
  -- 数据来源：QMT 行情 + akshare + 东方财富api
  -- AI 分析：Trading Agent（预留）
  -- API 框架：FastAPI
  -- 服务启动：Uvicorn
  -- 事件引擎：Threading + Queue
  -- 日志：Loguru
  -- SQLite（本地回测报告、策略配置、交易记录）
  -- JSON/YAML（配置文件）

### 前端
 -- Streamlit


### Python解释器路径
 -- D:/quant/venv/Scripts/python.exe

## 代码规范
-- 遵循PEP8

