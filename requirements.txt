# ============================================================
# alphaQuantSystem 依赖清单
# Python 版本: >=3.8, <3.11
# ============================================================

# ── 核心依赖 ──
numpy>=1.20
pandas>=1.3
loguru>=0.6
pyyaml>=6.0
requests>=2.25

# ── 回测引擎 ──
backtrader>=3.2.0

# ── 绩效分析 ──
quantstats>=0.0.63

# ── 数据源 ──
akshare>=1.10

# ── Web 监控面板（可选） ──
fastapi>=0.100
uvicorn>=0.23
streamlit>=1.25

# ── 测试 ──
pytest>=7.0

# ============================================================
# 以下依赖需要手动安装，不在 PyPI 上:
# ============================================================
# xtquant  — QMT 极速交易 Python SDK（随 QMT 客户端安装）
#   将 xtquant/ 目录复制到 Python site-packages 或项目根目录
